Index: Common/src/mobile/java/tk/glucodata/ui/calibration/CalibrationBottomSheet.kt
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/Common/src/mobile/java/tk/glucodata/ui/calibration/CalibrationBottomSheet.kt b/Common/src/mobile/java/tk/glucodata/ui/calibration/CalibrationBottomSheet.kt
--- a/Common/src/mobile/java/tk/glucodata/ui/calibration/CalibrationBottomSheet.kt
+++ b/Common/src/mobile/java/tk/glucodata/ui/calibration/CalibrationBottomSheet.kt
@@ -55,7 +55,7 @@
 import kotlinx.coroutines.flow.collectLatest
 import kotlinx.coroutines.launch
 import kotlinx.coroutines.withContext
-import tk.glucodata.GlucosePoint
+import tk.glucodata.ui.GlucosePoint
 import tk.glucodata.data.calibration.CalibrationEntity
 import tk.glucodata.data.calibration.CalibrationManager
 import tk.glucodata.logic.TrendEngine
Index: Common/src/mobile/java/tk/glucodata/ui/ComposeHost.kt
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/Common/src/mobile/java/tk/glucodata/ui/ComposeHost.kt b/Common/src/mobile/java/tk/glucodata/ui/ComposeHost.kt
--- a/Common/src/mobile/java/tk/glucodata/ui/ComposeHost.kt
+++ b/Common/src/mobile/java/tk/glucodata/ui/ComposeHost.kt
@@ -730,7 +730,7 @@
             initialValueAuto = initAuto,
             initialValueRaw = initRaw,
             initialTimestamp = initTime,
-            glucoseHistory = glucoseHistory.map { tk.glucodata.GlucosePoint(it.timestamp, it.value, it.rawValue) },
+            glucoseHistory = glucoseHistory,
             isMmol = unit.contains("mmol", ignoreCase = true),
             viewMode = viewMode,
             onNavigateToHistory = {
@@ -2711,8 +2711,7 @@
     // Calculate on the fly using the passed history subset
     val trendResult = remember(history, index) {
         val relevantHistory = if (history.isNotEmpty()) history.drop(index) else listOf(point)
-        val nativeList = relevantHistory.map { tk.glucodata.GlucosePoint(it.timestamp, it.value, it.rawValue) }
-        tk.glucodata.logic.TrendEngine.calculateTrend(nativeList, useRaw = (viewMode == 1 || viewMode == 3))
+        tk.glucodata.logic.TrendEngine.calculateTrend(relevantHistory, useRaw = (viewMode == 1 || viewMode == 3))
     }
     
     // Wrap in Column to place Divider below the Surface
@@ -4498,7 +4497,11 @@
                         Spacer(modifier = Modifier.height(48.dp))
                     }
                     }
-                Row(modifier = Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.spacedBy(8.dp)) {
+                FlowRow(
+                    modifier = Modifier.fillMaxWidth(),
+                    horizontalArrangement = Arrangement.spacedBy(8.dp),
+                    verticalArrangement = Arrangement.spacedBy(8.dp) // Add vertical spacing for wrapped rows
+                ) {
 
                     val autoStr = stringResource(R.string.auto)
                     val rawStr = stringResource(R.string.raw)
Index: Common/src/mobile/java/tk/glucodata/ui/DashboardComponents.kt
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/Common/src/mobile/java/tk/glucodata/ui/DashboardComponents.kt b/Common/src/mobile/java/tk/glucodata/ui/DashboardComponents.kt
--- a/Common/src/mobile/java/tk/glucodata/ui/DashboardComponents.kt
+++ b/Common/src/mobile/java/tk/glucodata/ui/DashboardComponents.kt
@@ -117,13 +117,11 @@
     // Advanced Trend
     val trendResult = remember(history, latestPoint) {
         if (history.isNotEmpty()) {
-             // Map Kotlin UI points to Native Java points for shared TrendEngine
-             val nativeList = history.map { tk.glucodata.GlucosePoint(it.timestamp, it.value, it.rawValue) }
-             tk.glucodata.logic.TrendEngine.calculateTrend(nativeList, useRaw = (viewMode == 1 || viewMode == 3))
+             // Use UI points directly for TrendEngine
+             tk.glucodata.logic.TrendEngine.calculateTrend(history, useRaw = (viewMode == 1 || viewMode == 3))
         } else if (latestPoint != null) {
             // Fallback
-             val nativeList = listOf(tk.glucodata.GlucosePoint(latestPoint.timestamp, latestPoint.value, latestPoint.rawValue))
-             tk.glucodata.logic.TrendEngine.calculateTrend(nativeList, useRaw = (viewMode == 1 || viewMode == 3))
+             tk.glucodata.logic.TrendEngine.calculateTrend(listOf(latestPoint), useRaw = (viewMode == 1 || viewMode == 3))
         } else {
             tk.glucodata.logic.TrendEngine.TrendResult(tk.glucodata.logic.TrendEngine.TrendState.Unknown, 0f, 0f, 0f, 0f)
         }
